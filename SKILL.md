---
name: gongwen-format
description: 按《市委常委会会议材料（汇报材料）格式规范》调整公文 Word 文档格式。适用于用户需要修改、格式化、统一公文 / 会议材料 / 汇报材料格式的场景，包括页面边距与页码、主副标题字体、正文各级标题与正文的字体字号行距、数字英文 Times New Roman 等。当用户说「帮我按公文格式排版」「修改这份材料格式」「按常委会会议材料格式调整」或提到公文格式规范时使用。
---

# 公文格式调整

按溧阳商务局《市委常委会会议材料（汇报材料）格式规范》，把一份公文 docx 调整为规范格式。

## 快速开始

核心是运行格式化脚本，它会自动完成绝大部分确定性的排版工作：

```bash
python scripts/apply_format.py <输入.docx> [输出.docx] [--title-font 字体名] [--no-cover]
```

- 不指定输出路径时，自动生成 `<输入>_formatted.docx`。
- `--title-font` 默认 `方正小标宋简体`；若目标机器字体名为 `方正小标宋_GBK`，用此参数覆盖。
- `--no-cover` 跳过封面标题区处理（仅格式化正文与页面），用于封面结构非标准的文档。

## 工作流

### 第 1 步：运行脚本

对用户指定的 docx 运行 `apply_format.py`。脚本自动完成：

1. **页面设置**：上 3cm、下 3cm、左 2.7cm、右 2.7cm、页脚 2.4cm。
2. **页码**：页脚插入「— 1 —」字段，四号宋体，居中。
3. **封面标题区**（正文起点之前的段落）：主标题二号方正小标宋、副标题三号楷体、落款三号楷体，均居中、35 磅行距；封面大标题（含「会议材料 / 汇报材料」字样）四号。
4. **正文层级**（按文本模式识别）：一级「一、」黑体、二级「（一）」楷体加粗、三级「1．」仿宋加粗、正文仿宋，均三号、28 磅行距。
5. **数字与英文字母**统一设为 Times New Roman（通过西文字体属性，与中文字体并存自动切换）。

### 第 2 步：核对脚本无法确定的细节

脚本处理后，需人工核对并修正以下三点（脚本无法 100% 判断）：

- **封面标题区**：脚本按「正文起点之前的段落 + 文本特征」启发式识别，若文档封面结构特殊（如多行主标题、额外说明文字），核对主标题 / 副标题 / 落款的归属是否正确。
- **页码「外侧」**：脚本生成的是居中页码；规范要求「外侧」（奇数页右侧、偶数页左侧），需在 Word 中启用「奇偶页不同」后手动调整为外侧。
- **正文首行缩进**：规范未明确缩进值，按材料实际需要保留或设置（公文惯例为首行缩进 2 字符）。

### 第 3 步：交付

把输出文件路径告知用户，并简要说明已应用的格式与需要用户最终确认的封面、页码、缩进项。

## 格式速查

| 元素 | 字体 | 字号 | 加粗 | 行距 |
|------|------|------|------|------|
| 主标题 | 方正小标宋简体 | 二号 22磅 | 否 | 35磅 |
| 副标题 / 落款 | 楷体_GB2312 | 三号 16磅 | 否 | 35磅 |
| 一级标题「一、」 | 黑体 | 三号 16磅 | 否 | 28磅 |
| 二级标题「（一）」 | 楷体_GB2312 | 三号 16磅 | 是 | 28磅 |
| 三级标题「1．」 | 仿宋_GB2312 | 三号 16磅 | 是 | 28磅 |
| 正文 | 仿宋_GB2312 | 三号 16磅 | 否 | 28磅 |
| 数字 / 英文 | Times New Roman | — | — | — |

层次序数：`一、` → `（一）` → `1．` → `（1）`

## 资源

- `scripts/apply_format.py` — 自动化格式化脚本（页面 + 正文层级 + 封面 + 数字英文）。
- `references/format-spec.md` — 完整格式规范（字号对照表、页面设置、字体名兼容性提醒、印刷要求），需要精确核对某项格式时读取。
